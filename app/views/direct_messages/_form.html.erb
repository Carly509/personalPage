<%= form_with(model: direct_message, local: true) do |form| %>
  <% if direct_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(direct_message.errors.count, "error") %> prohibited this direct_message from being saved:</h2>

      <ul>
        <% direct_message.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="col-md-6">
                          <div class="contact-info">
                              <div class="row">
                                <div class="form-group col-sm-6">
                                 
                                  <%= form.text_field :name, :required=>"", :placeholder =>"Name*"%>
                                </div>
                                <div class="form-group col-sm-6">
                                
                                   <%= form.email_field :mail, :required=>"", :placeholder =>"Email*"%>
                                </div>
                                <div class="form-group col-sm-12">
                                  
                                   <%= form.text_field :subject, :placeholder =>"Subject (Optinal)"%>
                                </div>
                                <div class="form-group col-sm-12">
                                 
                                   <%= form.text_area :Dm, :required=>"", :placeholder =>"Message*" %>
                                </div>
                                <div class="col-sm-12"><%= form.submit "Contact me", class: "btn"%></div>
                              </div>
                            </form>
                          </div>
                        </div>
<% end %>
